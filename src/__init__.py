"""Top-level package for the AccountingApp project.

This package initializes the Python package and may contain
package-wide configuration or metadata in the future.
Currently it is intentionally left minimal, serving only as
a marker for the Python interpreter that this directory should
be treated as a package. Additional initialization code can
be placed here if necessary.
"""
